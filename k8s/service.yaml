apiVersion: v1
kind: Service

metadata:
  name: krayin-service
  namespace: laravel-app

spec:
  type: NodePort

  selector:
    app: krayin-crm

  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080